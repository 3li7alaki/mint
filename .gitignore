# Dependencies
node_modules/
package-lock.json
bun.lockb
bun.lock

# mint local state (transient, per-session)
.mint/tasks/
.mint/research/
.mint/worktrees/
.mint/plugins/

# mint-ssh cache (contains fetched server IPs)
.mint/ssh-cache.json

# Local design docs (not published)
docs/plans/

# mint config files are committed:
# .mint/config.json — shared project settings
# .mint/hard-blocks.md — rules everyone follows
# .mint/issues.md, issues.jsonl — institutional knowledge
# .mint/wins.jsonl, patterns.jsonl, instincts.jsonl — learning logs
.mint/sessions/
.mint/.freeze-list.json
.mint/.browser-sessions.json
.mint/.gate-ledger.jsonl
.mint/.session-state.json
